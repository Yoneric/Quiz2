CREATE TABLE SALARIES(
  ID NUMBER(10),
  EMPLOYEE_ID NUMBER(10) NOT NULL,
  PROJECT_ID NUMBER(10),
  SALARY NUMBER(10,2) NOT NULL,
  SALARIES_PK PRIMARY KEY (ID)    
);
GO

CREATE TABLE PROJECTS(
  ID NUMBER(10),
  CODE VARCHAR(255 BYTE) NOT NULL,
  MANAGER_ID NUMBER(10),
  BUDGET NUMBER(10,2) NOT NULL,
  DATE_OF_BEGINNING DATE,
  DATE_OF_ENDING DATE,
  PROJECTS_PK PRIMARY KEY (ID)
);
GO

CREATE TABLE EMPLOYEES(
  ID NUMBER(10),
  MANAGER_ID NUMBER(10),
  DATE_OF_JOINING DATE NOT NULL,
  FULL_NAME VARCHAR(255 BYTE) NOT NULL,
  EMPLOYEES_PK PRIMARY KEY (ID)
);
GO

ALTER TABLE SALARIES
ADD CONSTRAINT FK_SALARY_EMPLOYEE
FOREIGN KEY (EMPLOYEE_ID)
REFERENCES EMPLOYEES (ID)

ALTER TABLE SALARIES
ADD CONSTRAINT FK_SALARY_PROJECT
FOREIGN KEY (PROJECT_ID)
REFERENCES PROJECTS (ID)

ALTER TABLE PROJECTS
ADD CONSTRAINT FK_MANAGER_PROJECT
FOREIGN KEY (MANAGER_ID)
REFERENCES EMPLOYEES (ID)

ALTER TABLE EMPLOYEES
ADD CONSTRAINT FK_MANAGER
FOREIGN KEY (MANAGER_ID)
REFERENCES EMPLOYEES (MANAGER_ID)
---- Queries:

--1
SELECT COUNT(*) FROM PROJECTS
WHERE CODE='48205501099';

--2
SELECT FULL_NAME FROM EMPLOYEES JOIN SALARIES
ON ID = EMPLOYEE_ID
WHERE SALARIES.SALARY BETWEEN 2500 AND 3000